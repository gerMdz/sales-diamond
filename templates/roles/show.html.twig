{% extends 'base.html.twig' %}

{% block title %}Roles Ver{% endblock %}

{% block body %}
    <h1>Roles ver</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ role.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ role.nombre }}</td>
            </tr>
            <tr>
                <th>Identificador</th>
                <td>{{ role.identificador }}</td>
            </tr>
            <tr>
                <th>Descripción</th>
                <td>{{ role.descripcion }}</td>
            </tr>
            <tr>
                <th>Activo?</th>
                <td>{{ role.isActivo ? 'Si' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_roles_index') }}">Listado</a>

    <a href="{{ path('app_roles_edit', {'id': role.id}) }}">Editar</a>

    {{ include('roles/_delete_form.html.twig') }}
{% endblock %}
