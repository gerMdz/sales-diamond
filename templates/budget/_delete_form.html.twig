{#<form method="post" action="{{ path('app_budget_delete', {'id': budget.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">#}
{#    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ budget.id) }}">#}
{#    <button class="btn">Delete</button>#}
{#</form>#}
